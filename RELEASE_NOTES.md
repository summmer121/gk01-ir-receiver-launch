## v1.2.0 (2026-06-30)

### 新增
- 🚫 **名称查重**：接收映射值与发送按键名现在全局唯一
  - 「接收映射写入」：映射值若已被占用（无论是其它映射还是发送按键），拒绝保存（编辑同一红外码可覆盖自身）
  - 「学习发送写入」：按键名若已被占用，拒绝学习
  - 重名时「红外码状态」显示 `名称重复：xxx——已存在，请改名`，并写入日志

### 实现
- `ir_store.h` 新增 `name_taken(name, exclude_ir_code)` 全局查重函数，跨 keymap + slots 两套存储检查

### 固件
- `gk01_0630_v1.2.0.bin` — ESP12F，OTA 已实测成功（config_hash=0xbfbfd1f2）
- MD5: 75201d061099168b9df1682e9844b948
